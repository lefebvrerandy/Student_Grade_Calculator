<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.randy_bence.student_grade_calculator">
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainMenu">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".FinalGradeCalculator"
            android:label="@string/title_activity_final_grade_calculator"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".ConvertGrade"></activity>
        <activity android:name=".PreviousSemesterView"></activity>
        <activity android:name=".SupportPage"></activity>
        <activity android:name=".ViewGrades"></activity>
    </application>

</manifest>